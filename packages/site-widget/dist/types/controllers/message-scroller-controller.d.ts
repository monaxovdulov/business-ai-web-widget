import type { ReactiveController, ReactiveControllerHost } from "lit";
export declare const SCROLL_EDGE_THRESHOLD = 8;
export declare const PREVIOUS_ITEM_PEEK = 40;
export declare const SMOOTH_SCROLL_CLEAR_MS = 180;
export declare const SCROLL_EPSILON = 0.5;
export type MessageScrollerMode = "following-bottom" | "free-scrolling" | "anchored-to-message" | "settling-jump";
export type MessageScrollerItem = {
    id: string;
    scrollAnchor: boolean;
};
export type MessageScrollerSnapshot = {
    mode: MessageScrollerMode;
    canScrollStart: boolean;
    canScrollEnd: boolean;
    newItemCount: number;
};
export type MessageScrollerElements = {
    root?: HTMLElement;
    viewport: HTMLElement;
    content: HTMLElement;
    tailSpacer: HTMLElement;
};
export type MessageScrollerScrollOptions = {
    behavior?: ScrollBehavior;
};
export declare class MessageScrollerController implements ReactiveController {
    private readonly host;
    private root;
    private viewport;
    private content;
    private tailSpacer;
    private items;
    private mode;
    private snapshot;
    private newItemCount;
    private hasInitialPlacement;
    private activeAnchorId;
    private pendingLayoutAnchor;
    private pendingReconcile;
    private resizeObserver;
    private observedRows;
    private frameId;
    private programmaticScroll;
    private programmaticClearTimer;
    private settlingTimer;
    private pointerActive;
    private lastScrollTop;
    constructor(host: ReactiveControllerHost);
    hostUpdate(): void;
    hostDisconnected(): void;
    hostConnected(): void;
    connect({ root, viewport, content, tailSpacer }: MessageScrollerElements): void;
    reconcile(items: readonly MessageScrollerItem[]): void;
    scrollToEnd(options?: MessageScrollerScrollOptions): boolean;
    scrollToMessage(messageId: string, options?: MessageScrollerScrollOptions): boolean;
    disconnect(): void;
    getSnapshot(): MessageScrollerSnapshot;
    private scheduleCommit;
    private commitReconcile;
    private startTurnAnchor;
    private reconcileActiveAnchor;
    private requiredTailHeight;
    private desiredScrollTop;
    private restoreLayoutAnchor;
    private captureFirstVisible;
    private getAppendedItems;
    private isPrepend;
    private findSequenceStart;
    private handleWheel;
    private handleTouchMove;
    private handleKeydown;
    private handlePointerDown;
    private handlePointerUp;
    private handleScroll;
    private releaseForUser;
    private performScroll;
    private markProgrammaticScroll;
    private handleWindowResize;
    private updateSnapshot;
    private distanceToEnd;
    private setTailHeight;
    private findRow;
    private hasLayout;
    private normalizeBehavior;
    private isInteractive;
    private reconcileObservedRows;
    private commitModeAttribute;
    private detachElements;
    private clearProgrammaticTimer;
    private clearSettlingTimer;
    private requestFrame;
    private cancelFrame;
}
//# sourceMappingURL=message-scroller-controller.d.ts.map